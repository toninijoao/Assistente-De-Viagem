🧭 Assistente de Viagem com API Gemini

Este projeto é um Assistente de Viagem interativo em Python, que utiliza a API Gemini (Google AI) para fornecer informações inteligentes sobre rotas, distâncias e custos de viagem.

🚀 Funcionalidades

Calcula automaticamente a distância por estradas entre duas cidades.

Obtém a autonomia média do carro informado via API Gemini.

Estima o consumo total de combustível e o custo aproximado da viagem (baseado no preço médio da gasolina).

Exibe as principais cidades ao longo da rota (em formato JSON).

Calcula o tempo estimado de viagem considerando velocidade média de 110 km/h.

🧠 Uso da API Gemini

O programa se comunica com o modelo gemini-2.5-flash, processando prompts em linguagem natural para:

Obter distâncias e autonomia do veículo.

Gerar rotas detalhadas em formato JSON.

Realizar interpretações textuais e numéricas de forma dinâmica.

💻 Execução

O usuário interage pelo terminal, informando origem, destino e veículo.
A API Gemini interpreta as solicitações e retorna dados processados, que o script converte em resultados claros e úteis para o viajante.

Um projeto simples, mas poderoso, que combina Python + IA generativa para criar uma experiência de viagem inteligente.

////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////

🧭 Travel Assistant with Gemini API

This project is an interactive Travel Assistant built with Python, powered by the Google Gemini API to provide smart travel insights such as routes, distances, and trip costs.

🚀 Features

Automatically calculates road distance between two cities.

Retrieves the average fuel efficiency of the selected car using the Gemini API.

Estimates total fuel consumption and trip cost (based on average gasoline price).

Displays the main cities along the route (in JSON format).

Calculates the estimated travel time assuming an average speed of 110 km/h.

🧠 Gemini API Usage

The program communicates with the gemini-2.5-flash model to:

Retrieve distances and vehicle autonomy.

Generate detailed driving routes in JSON format.

Interpret natural language prompts and extract numerical data dynamically.

💻 How It Works

The user interacts through the terminal, entering origin, destination, and vehicle information.
The Gemini API processes these prompts and returns structured responses, which the script converts into clear, useful travel data.

A simple yet powerful project combining Python + Generative AI to deliver a smart, conversational travel experience.
